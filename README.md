# intro_mobile_robotics

Lab material for the **Introduction to Mobile Robotics** course.

## Getting the Docker image

A pre-built Docker image is available for this tutorial.

Pull the image with:

```bash
docker pull ghcr.io/fabio-amadio/turtlebot3-sim:2026
```

After the image has been downloaded, start the container and open a terminal inside it by running:

```bash
bash run_tb3.sh
```
